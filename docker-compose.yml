version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "8080:80"

  backend-build:
    build:
      context: .
      dockerfile: backend/Dockerfile
